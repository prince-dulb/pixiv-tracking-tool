# 开发计划书

> 上次更新：2026-07-03 | 当前版本：v0.0.7

---

## 一、版本路线总览

| 版本     | 主题                                          | 描述     |
| ------ | ------------------------------------------- | ------ |
| v0.0.4 | 进度条重构 + 多任务并发 + UI 完善                       | 基础构建   |
| v0.0.5 | API 节流                                      | 解决网络问题 |
| v0.0.6 | 隐藏/删除 + 收藏 + 翻页 + 筛选栏重构 + 投稿期间 + Tag 筛选     | 基础构建   |
| v0.0.7 | IPv6 外网访问 + 端口设置 + Bug 修复 + 卡片角标 + 画师改名自动同步 | 小修小补   |
| v0.0.8 | 自定义排序 + Tag 图表 + 备份还原 + 工具页                 | *工作中*  |
| v0.0.9 | 系统托盘                                        |        |

---

## 二、需求列表

| 需求            | 描述                                                                                                                                                                                                                               | 状态     | 分配版本   |
| ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------ |
| 核心功能框架        | 画师搜索与特别关注、gallery-dl 全量下载、CSS Grid 画廊（画师/类型多选筛选+反选）、作品详情页（原图+前后导航+键盘+触屏）、定时检查、文件级下载进度条+多任务并发、画师管理（暂停/恢复/刷新）、设置页（统计+手动检查+数据目录迁移）、Ugoira→GIF、SQLite WAL、非阻塞添加画师                                                                    | ✅ 已实现  | v0.0.4 |
| API 节流与限速     | Pixiv API 全局节流（~1 req/s 安全基线 + token 过期自动刷新 + rate limit 60s/120s/180s 退避）、gallery-dl 同步限速、文件系统安全画师名                                                                                                                             | ✅ 已实现  | v0.0.5 |
| 收藏与翻页体系       | 收藏/取消收藏（详情页/pv 右下角+卡片 hover 角标，AJAX 乐观更新+toast，全量同步入口，下载时自动内联同步）、画廊翻页（无限滚动+页码翻页，600px预加载+1s冷却+上限封顶）、筛选栏重构（☰浮动面板→4section手风琴，AJAX pushState 不刷新）、投稿期间筛选（不限/24h/周/月/半年/年+自定义）、Tag 筛选（聚合 chips+自由输入，OR 逻辑，独立 illustration_tag 表+索引） | ✅ 已实现  | v0.0.6 |
| 网络与体验优化       | IPv6 双栈外网访问（IPv4 0.0.0.0 + IPv6 ::/IPV6_V6ONLY）、端口设置（设置页输入框+持久化到 .env）、多p图页码自然排序修复、卡片多p角标+漫画类型边框、画师改名自动同步（刷新时比对 API→重命名目录→更新 DB→替换 file_paths）                                                                                   | ✅ 已实现  | v0.0.7 |
| 自定义排序         | 画廊排序方式可选：按发布时间（默认）/ 下载时间 / 收藏数 / 画师名 / Tag 热度。升序 / 降序。Tag 热度定义：作品所有 tag 中取出现次数最多的 tag 的频率值作为 tag_score。筛选面板新增"排序" section，复用现有手风琴面板模式                                                                                            | 📋 已规划 | v0.0.8 |
| Tag 频率统计 + 图表 | 后端 GROUP BY tag 查询所有已下载作品的 tag 出现次数（60s 缓存）。Chart.js CDN 横向条形图，Top N 可配置（10/20/50/100）                                                                                                                                           | 📋 已规划 | v0.0.8 |
| 备份 / 还原       | 备份：shutil.make_archive 打包 DATA_ROOT 为 zip，文件名带时间戳。还原：解压到临时目录→对比 DB+文件数→展示摘要。冲突处理：覆盖当前 / 取消 / 合并（去重合并，同名图片取较新的）                                                                                                                   | 📋 已规划 | v0.0.8 |
| 新增"工具"页       | 导航从 `作品 · 画师 · 设置` 改为 `作品 · 画师 · 工具 · 设置`。工具页定位：既不浏览作品、也不改配置的后台功能，放置 Tag 统计图表、备份/还原入口                                                                                                                                            | 📋 已规划 | v0.0.8 |
| 系统托盘          | PyInstaller console=False → 无命令行窗口。pystray 托盘图标，右键菜单：打开浏览器 / 设置 / 退出。控制台输出→日志文件。启动后 Web 服务后台运行                                                                                                                                   | 📋 已规划 | v0.0.9 |
| 应用内更新         | 查 GitHub API 拿最新 release → 有新版本提示下载 → bat 脚本覆盖 exe 重启。启动时自动查 + 工具页手动按钮                                                                                                                                                           | 待讨论    | -      |
| 图片翻译          | 集成 manga-image-translator（GPL-3.0，子进程 CLI），OCR→日→中翻译→渲染回填，首次需下载 ~1-2GB 模型                                                                                                                                                        | 待讨论    | -      |
| 评分系统          | rating 字段已有但无 UI                                                                                                                                                                                                                 | 待讨论    | -      |
| 批量操作          | 多选作品 → 批量隐藏/删除/收藏（长按/角标触发，不走工具页）                                                                                                                                                                                                 | 待讨论    | -      |
| 深色模式          | 项目本身深色主题，浏览器暗色模式自适应。做亮色切换 ROI 极低                                                                                                                                                                                                 | 搁置     | -      |

---

## 三、已实现

### v0.0.4（2026-05-24）

画师搜索与特别关注、gallery-dl 全量下载、CSS Grid 画廊（画师/类型多选筛选+反选）、作品详情页（原图+前后导航+键盘+触屏）、定时检查、文件级下载进度条+多任务并发、画师管理（暂停/恢复/刷新）、设置页（统计+手动检查+数据目录迁移）、Ugoira→GIF、SQLite WAL、非阻塞添加画师。

### v0.0.5（2026-06-16）

Pixiv API 全局节流（~1 req/s 安全基线 + token 过期自动刷新 + rate limit 60s/120s/180s 退避）、gallery-dl 同步限速、文件系统安全画师名。

### v0.0.6（2026-06-16）

| 模块 | 内容 |
|------|------|
| 收藏/取消收藏 | 详情页/pv 右下角+卡片 hover 角标，AJAX 乐观更新+toast，全量同步入口，下载时自动内联同步，`restrict="public"` |
| 画廊翻页 | 默认无限滚动（IntersectionObserver+Fragment），可选页码翻页（`?page=N`+页码栏），600px预加载+1s冷却+图片感知+上限封顶，回到顶部按钮 |
| 筛选栏重构 | ☰浮动面板→4section手风琴（画师/类型/期间/显示），面板 AJAX 化（pushState 不刷新），筛选条件翻页间保持 |
| 投稿期间筛选 | 预设：不限/24h/周/月/半年/年 + 自定义起始~截止日期 |
| Tag 筛选 | 聚合 chips + 自由输入，OR 逻辑，默认全选（对齐画师行为），详情页 tag 可点击，独立 `illustration_tag` 表+索引 |
| 性能优化 | Fragment 提前返回（AJAX 免 count/tag 聚合），tag/artist 30s 缓存，sessionmaker 复用，SSL/连接重试 |
| Bug 修复 | `_post` token 刷新、`restrict` 参数格式、filter-after-limit 顺序、tag 迁移幂等 |

### v0.0.7（2026-06-22）

| 模块 | 内容 |
|------|------|
| IPv6 外网访问 | 双 socket（IPv4 `0.0.0.0` + IPv6 `::`/`IPV6_V6ONLY`），启动打印可访问链接，修复 `run.py` 硬编码端口 |
| 端口设置 | 设置页新增端口号输入框，保存到 `.env`，提示重启 |
| Bug 修复 | 多p图 ≥10 页时页码排序混乱（`sorted()` → `_natsort_key` 自然排序） |
| 卡片角标 | 多p作品卡片左下角显示页数（`page_count > 1`），漫画类型包裹主题色边框 |
| 画师改名同步 | 刷新时比对 Pixiv API 画师名，变更则重命名目录 + 更新 DB + 替换 file_paths；新旧目录共存时合并去重 |

---

## 四、版本号与 Git 约定

- 版本号格式：`v{major}.{minor}.{patch}`
- 从 master 开 worktree 分支开发
- 开发完成 → 合并 master + 打 tag + GitHub Release
- release 目录：`release/vX.Y.Z/`，包含 `.exe`
- 构建命令：`cd app && pyinstaller build.spec --distpath ../release/vX.Y.Z --workpath build_tmp --clean`

---

## 五、架构注意事项

- SQLite 单文件，多线程 WAL 模式
- 无前端框架，Jinja2 SSR + 原生 JS
- 后台任务 `threading.Thread(daemon=True)`
- 图片 `FileResponse` + `loading="lazy"`
- CSS 变量 `:root` 统一管理主题色
- 筛选状态全走 URL 参数，前端 AJAX + pushState
- Fragment 模板（`gallery_fragment.html` / `artist_fragment.html`）供 AJAX 无限滚动
- `illustration_tag` 独立表 + 索引，JSON tags 列保留兼容
