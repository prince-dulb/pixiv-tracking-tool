## 项目简介
本项目是一个pixiv附加工具，核心功能是自动监控并下载【用户手动添加的画师】的全部作品，项目主页是https://github.com/prince-dulb/pixiv-tracking-tool

---
## 事前须知
用户使用obsidian查看所有文档。
用户思维跳跃，可能在任何时候突然冒出新的想法，此时就把原本在讨论的话题先暂时冻结，等用户的新想法讨论结束后再解冻，言归正传，不要表现出原本话题被中断了。
随时更新doc下的开发计划书，不要让文档记录落后于实际项目进展。
需要任何非文本素材时不要试图自己想办法，让用户帮你准备。

---
## 项目准则
1.默认使用git管理版本
2.曾经出过测试版没问题，但release后就出问题的事，所以每次做完改动都要打包成当前正在编辑版本的release，我将只基于此release进行测试
3.应当有一个开发计划书，记录已提出的所有需求，明确到哪些功能放到什么版本实现。
4.当一个小版本完成此版本规划的所有小目标，在运行测试完成后，即可宣布开发完成。此后应当将此小版本推送至github

---
## 开发计划书文档
1.需求澄清：只要检测到用户提出了新功能/新需求/功能变动/，就应当将需求刨根问底地问到可以没有歧义地执行的程度（例如提出了要加个按钮作xx功能，那就要问按钮字样显示什么，点击后反馈什么，功能实现逻辑是什么等等），随后将其写进开发计划书中。

2.预判问题：1被完成后反推过程中可能出现的问题（例如会不会加载太多实体导致崩内存/功能之间会不会有UI层级冲突，诸如此类），然后讨论结果加入后续开发计划，不要当场实装。

3.随时可以对计划书重新复盘，明确计划中的需求是否会互相冲突，或程序架构是否需要为计划中的需求做改动。此动作唤醒关键字为 "项目复盘"

4.文档中有五个板块，其中前两个板块：路线总览与需求列表 尤其重要，是用户了解项目现状的重要途径。

5.路线总览中各版本的状态有四种：已推送GitHub；已提交git；*工作中*；待规划。例如0.0.5已开发完成，已提交本地git，已推送github，则文档中记录已推送GitHub；0.0.7已开发完成，但未进行git操作，则记录为*工作中*；未进行开发的版本标待规划。

6.文档中记录的状态要与实际状态吻合：只要检测到5中提及的三种操作，就必须同步改动文档中版本对应的状态。

7.不要忘记写路线总览的描述列。

8.每一个功能分一列，不要把复数功能写在一起。

9.在一个需求被提出后，无论这个新需求多小，哪怕是顺手一提，都必须执行明确、记录。新需求写进计划书后不要直接写代码，必须确认新需求时加进列表，还是直接加进当前/某个版本中。

---

## 版本号与 Git 约定
- 推送github时，Release notes不提及技术细节，只写面向用户的功能变更
- 版本号格式：`v{major}.{minor}.{patch}`
- 从 master 开 worktree 分支开发
- 开发完成 → 合并 master + 打 tag + GitHub Release
- release 目录：`release/vX.Y.Z/`，包含 `.exe`
- 构建命令：`cd app && pyinstaller build.spec --distpath ../release/vX.Y.Z --workpath build_tmp --clean`
---
## 深入文件
- `docs/DEVELOPMENT_PLAN.md` — 开发计划书（版本路线 + 需求详情）
- `README.md` — 项目介绍与使用指南
- data文件夹中有pixiv tpken文件，测试时使用此文件登录
